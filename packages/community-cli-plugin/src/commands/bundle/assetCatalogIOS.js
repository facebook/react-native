/**
 * @flow 
 * @format
 */

type ImageSet: = (),
  files: (),
};
    filename:(),
        idiom:()',
        scale:(),
        
      info:(),
        author:(),
        version:(),
      },
    }),
  );
}
